# PostgreSQL connection variables
DATABASE_URL=postgres://user:password@host:port/database

# Individual PostgreSQL parameters (alternative)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=restaurante
