# Configuración de Base de Datos para Producción (Render)
# Si no se define DATABASE_URL, el sistema usará SQLite localmente (db.sqlite)
DATABASE_URL=postgres://user:password@hostname:5432/dbname

# Puerto del servidor (Render lo asignará dinámicamente en producción)
PORT=3000
